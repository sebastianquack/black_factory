<%= form_for(@username_score) do |f| %>
  <% if @username_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@username_score.errors.count, "error") %> prohibited this username_score from being saved:</h2>

      <ul>
      <% @username_score.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :score %><br />
    <%= f.number_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
