<%= form_for(@username_cookie) do |f| %>
  <% if @username_cookie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@username_cookie.errors.count, "error") %> prohibited this username_cookie from being saved:</h2>

      <ul>
      <% @username_cookie.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cookiehash %><br />
    <%= f.text_field :cookiehash %>
  </div>
  <div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :username %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
